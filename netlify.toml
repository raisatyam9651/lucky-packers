[build]
  command = "php -S localhost:8000 & sleep 5 && php build.php"
  publish = "dist"

[build.environment]
  PHP_VERSION = "8.2"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = false

[dev]
  command = "php -S localhost:8000"
  port = 8000
  publish = "dist"
